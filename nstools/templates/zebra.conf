hostname {{cfg['router']}}
password zebra
enable password zebra
log file /tmp/{{cfg['router']}}.log informational
log stdout
!
interface lo
!
{% for item in cfg['interfaces'] %}
interface {{item['name']}}
{% for addr in item['v4'] %}
 ip address {{addr}}
{% endfor %}
!
{% endfor %}
!
ip forwarding
!
line vty
!
